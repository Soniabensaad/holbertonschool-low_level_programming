#include "main.h"

/**
 * main - prints name, followed by a new line
 *
 * @argc : integer
 * @argv : array of strings
 *
 * Description: a program that prints name, followed by a new line.
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
argc = 0;
printf("%s\n", argv[argc]);
return (0);
}
